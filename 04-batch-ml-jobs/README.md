# 04 — Batch ML jobs

> Status: scaffolded.

## Objectives
- Run a sklearn training as a Kubernetes `Job`.
- Persist the trained artifact to a PVC.
- Schedule a nightly retrain with `CronJob`.
- Use parallelism / completions for hyperparameter sweeps.

## Try next
`../05-serving-single-model/`
