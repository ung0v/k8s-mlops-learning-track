# 02 — Storage & configuration

> Status: scaffolded.

## Objectives
- Persist data with PV / PVC (local-path storage class already in kind).
- Run a StatefulSet and see stable identity + storage.
- Inject config via ConfigMap and Secret (env + volume mount).
- Use an init container to prep data before the main app starts.

## Try next
`../03-packaging-ml-apps/`
